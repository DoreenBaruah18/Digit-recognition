# Digit-recognition
Digit recognition using machine learning is a technique where a computer is trained to identify handwritten or printed digits(0-9) from images.A model (usually a neural network)learns patterns from labeled data,such as pixel values of digit images.After training,the model can accurately predict the digit in new,unseen images. 
